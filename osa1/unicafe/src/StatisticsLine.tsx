const StatisticsLine = (props: any) => {
    /// ...
    return (
        <tr>
            <td>{props.text}</td>
            <td>{props.value}</td>
        </tr>
    )
}

export default StatisticsLine